// tsconfig.server.json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "es2020",
    "outDir": ".", // Output the compiled file to the root directory
    "noEmit": false // This must be false to create the .js file
  },
  "include": [
    "server.ts" // Only compile this file with these settings
  ]
}
